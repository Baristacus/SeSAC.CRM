{% extends 'base.jinja2' %}
{% block content %}
<h1>CRM Project: Seokin Yoon</h1>
<p>CRM Project Main Page</p>

<div class="card my-3">
    <div class="card-header">주요 기능</div>
    <ul class="list-group list-group-flush">
        <a href="{{ url_for('user.get_users') }}"
            class="list-group-item list-group-item-action d-flex justify-content-between align-items-start"
            aria-current="true">
            <div class="ms-2 me-auto">
                <div>
                    <span class="text-primary fw-bold">사용자</span> 목록 조회 <span>(사용자 등록 수: {{ '{:,.0f}'.format(user_count)
                        }})</span>
                </div>
            </div>
        </a>
        <a href="{{ url_for('store.get_stores') }}"
            class="list-group-item list-group-item-action d-flex justify-content-between align-items-start"
            aria-current="true">
            <div class="ms-2 me-auto">
                <div>
                    <span class="text-primary fw-bold">매장</span> 목록 조회 <span>(매장 등록 수: {{ '{:,.0f}'.format(store_count)
                        }})</span>
                </div>
            </div>
        </a>
        <a href="{{ url_for('item.get_items') }}"
            class="list-group-item list-group-item-action d-flex justify-content-between align-items-start"
            aria-current="true">
            <div class="ms-2 me-auto">
                <div>
                    <span class="text-primary fw-bold">상품</span> 목록 조회 <span>(상품 등록 수: {{ '{:,.0f}'.format(item_count)
                        }})</span>
                </div>
            </div>
        </a>
        <a href="{{ url_for('order.get_orders') }}"
            class="list-group-item list-group-item-action d-flex justify-content-between align-items-start"
            aria-current="true">
            <div class="ms-2 me-auto">
                <div>
                    <span class="text-primary fw-bold">주문</span> 목록 조회 <span>(주문 등록 수: {{ '{:,.0f}'.format(order_count)
                        }})</span>
                </div>
            </div>
        </a>
        <a href="{{ url_for('orderitem.get_orderitems') }}"
            class="list-group-item list-group-item-action d-flex justify-content-between align-items-start"
            aria-current="true">
            <div class="ms-2 me-auto">
                <div>
                    <span class="text-primary fw-bold">전체 주문</span> 목록 조회 <span>(전체 주문 등록 수: {{
                        '{:,.0f}'.format(orderitem_count)
                        }})</span>
                </div>
            </div>
        </a>
    </ul>
</div>
{% endblock %}